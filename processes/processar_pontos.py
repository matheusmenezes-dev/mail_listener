def processar_pontos(path_pdf):
    # names = extract_names_from_pdf(path_pdf)
    # for name in names
    #    info = scrape_info(name)
    #    dispatcher.create_mail(info) 
    #    mail_dispatcher.add_pending_mail()
    # mail.dispatcher.send_mails()
    return True